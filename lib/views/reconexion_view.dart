import 'package:flutter/material.dart';

class ReconexionView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Reconexión'),
      ),
      body: Center(
        child: Text('Aquí irá la funcionalidad para reconexión.'),
      ),
    );
  }
}
