import 'package:flutter/material.dart';

class LecturaView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Lectura de Medidores'),
      ),
      body: Center(
        child: Text('Aquí irá la funcionalidad para la lectura de medidores.'),
      ),
    );
  }
}
