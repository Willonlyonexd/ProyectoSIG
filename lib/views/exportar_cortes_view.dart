import 'package:flutter/material.dart';

class ExportarCortesView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Exportar Cortes al servidor'),
      ),
      body: Center(
        child: Text('Aquí irá la funcionalidad para exportar los cortes al servidor.'),
      ),
    );
  }
}
