import 'package:flutter/material.dart';

class RegistrarCortesView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Registrar Cortes'),
      ),
      body: Center(
        child: Text('Aquí irá la funcionalidad para registrar los cortes.'),
      ),
    );
  }
}
